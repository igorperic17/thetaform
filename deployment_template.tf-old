terraform {
  required_providers {
    theta = {
      source = "hashicorp.com/edu/theta"
    }
  }
}

resource "theta_deployment_template" "example" {
  name            = "llama38byqyf51glw5"
  project_id      = "prj_2tcq81a5wyk3637caigcn9ytrz2s"
  description     = "Sample description"
  container_images = "vllm/vllm-openai"  
  container_port  = "8000"
  vm_id               = "vm_c1"
  
  env_vars = jsonencode({
    HUGGING_FACE_HUB_TOKEN = "<your_token_here>"
  })
  
  tags = ["LLM", "API"]
}

